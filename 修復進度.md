# 修復進度

## 2026-06-24 16:14 遊玩中崩潰：Prefab 結構檔名被翻譯

### 問題
- 崩潰報告：`C:\Users\vm060\Desktop\错误报告-2026-6-24_下午_04.14.42.zip`
- 遊戲可進入並遊玩，但在遊玩中崩潰。
- Crash stack 指向 `prefab`：
  - `com.night.star.prefab.utils.RenderBlocksUtils.getBlockPos`
  - `com.night.star.prefab.utils.RenderBlocksUtils.buildBoundingBox`
  - `com.night.star.prefab.event.ClientSelectionRenderer.onRenderLevel`
- `latest.log` 顯示 Prefab 嘗試讀取不存在的結構檔：
  - `schematics\紈瓣...V2.nbt`
- 根因：翻譯軟體把 `.nbt` 結構檔名或資源引用翻譯/污染，導致模組執行時找不到原本的結構檔。

### 修復
- 在 `gui/main_window.py` 新增結構/資源引用判斷：
  - `.nbt`
  - `.schem`
  - `.schematic`
  - `.mcstructure`
  - 常見資源檔與命名空間路徑
- `should_translate()` 遇到結構/資源引用會直接跳過，不送翻譯引擎。
- `validate_translation()` 遇到結構/資源引用一律回原文，避免快取或記憶池污染再次套用。
- `get_translation()` 對快取/記憶池命中也會再跑驗證，避免舊污染值回到輸出。
- `_lang_value_needs_update()` 會偵測既有 zh 檔中的污染結構引用，輸出合併時還原為原始英文檔名。
- 修正 URL 判斷，避免 Patchouli 段落中含 URL 巨集時整段被誤判為不可翻譯。

### 測試
- 新增 `StructuralReferenceGuardTests`：
  - 確認 `HouseV2.nbt` 不會被翻譯。
  - 確認 `prefab:schematics/HouseV2.nbt` 不會被翻譯。
  - 確認已污染的 `房屋V2.nbt` 會在合併輸出時還原為 `HouseV2.nbt`。
- 驗證指令：
  - `python -m unittest tests.test_translation_core.StructuralReferenceGuardTests`
  - `python -m unittest tests.test_translation_core`
  - `python -m py_compile gui\main_window.py tests\test_translation_core.py`
- 已用 PyInstaller 重新打包 `MinecraftTranslatorGUI.exe`，並做短暫啟動 smoke test。
- 結果：全部通過。

## 2026-06-24 23:11 Aetherium Protocol 書本/手冊殘留英文與 fallback 漏翻

### 問題
- 模組包：`C:\Users\vm060\Desktop\mc\.minecraft\versions\Aetherium Protocol`
- 原始包只讀分析結果：
  - `237` 個 JAR
  - `2,496` 個語言資源
  - `10,874` 個 Patchouli / book / manual 類資源
- 影片中可見問題包含：
  - 物品名稱、敘述中英文混在一起。
  - 部分書本頁面仍是英文。
  - 動物/手冊類書本可能顯示空白，或實際讀取 `en_us` fallback 而不是 `zh_tw`。
- 根因分成三類：
  - `zh_cn` 書本 fallback 存在時，掃描階段會把英文 `en_us` 來源跳過，導致完整英文文本沒有送翻譯。
  - 既有 `zh_tw` 書本修復只寫回 `zh_tw`，但部分模組實際渲染仍讀 `en_us` fallback。
  - Patchouli/手冊 JSON 輸出時，如果快取或記憶池暫時沒有譯文，`merge_structured_json_with_existing_zh()` 會把原英文可見欄位寫入 `zh_tw`，造成重新分析/翻譯後仍殘留英文。

### 修復
- `core/analysis_scan.py`
  - 當同一書本同時有 `en_us` 與 `zh_cn` 時，保留 `en_us` 作為完整翻譯來源。
  - 只有非英文來源才走 `zh_cn` repair fallback，避免英文書本被誤跳過。
- `translation_packager.py`
  - 新增 `translated_repair_fallback_paths()`。
  - 對 Patchouli、book、manual 的 `zh_tw` 修復結果，同步產生對應 `en_us` fallback 路徑。
- `core/jar_patcher.py`
  - JAR 直接套用時，書本/手冊 repair 結果會同時寫入 `zh_tw` 與安全的 `en_us` fallback。
  - 結構欄位、配方、物品 ID、類別 ID 不會被翻譯。
- `core/translation_flow.py`
  - 資源輸出流程也同步套用 `en_us` fallback repair。
  - Patchouli/手冊 JSON 寫出前，會再檢查可見欄位是否仍是英文或中英混雜；若有，先小批次補翻再輸出。
- `gui/main_window.py`
  - 新增 `_collect_untranslated_visible_json_strings()`，只收集可見文字，不碰 `type`、`item`、`category`、recipe/resource ID。
  - 新增 `_repair_structured_book_json_output()`，避免英文手冊 JSON 被寫進 `zh_tw`。
  - 偵測到輸出中仍含原始英文片段時，視為未翻譯並補翻，改善「中文+英文混在一起」。

### 崩潰防線
- 這次修復只處理語言、Patchouli/book/manual 可見文字。
- 不修改 `.class`、配方、config、NBT 結構檔、資源 ID、物品 ID、category/type/recipe 等技術欄位。
- `en_us` fallback 只對已翻譯的手冊/書本覆蓋，目的為支援實際渲染仍讀 fallback 的模組；不改模組邏輯。
- 對伺服器包而言，這類輸出仍屬語言/手冊文字資源；不應影響伺服器載入邏輯。若未來發現 crash stack 指向 recipe/config/class/NBT，必須視為高風險並保留原文。

### 測試
- 新增/更新測試：
  - `AnalysisScanFilterTests.test_book_txt_scan_keeps_en_us_when_zh_cn_exists`
  - `PackagerTests.test_legacy_lang_and_translated_path`
  - `PatchouliJsonTranslationTests.test_untranslated_patchouli_output_detection_keeps_ids_safe`
  - `PatchouliJsonTranslationTests.test_structured_book_json_retries_untranslated_visible_fields_before_output`
  - `JarPatcherTests.test_high_risk_jar_allows_assets_language_only_patch`
- 驗證指令：
  - `python -m unittest tests.test_translation_core.PatchouliJsonTranslationTests tests.test_translation_core.PackagerTests tests.test_translation_core.AnalysisScanFilterTests tests.test_translation_core.JarPatcherTests`
  - `python -m unittest tests.test_translation_core`
  - `python -m py_compile gui\main_window.py translation_packager.py core\analysis_scan.py core\jar_patcher.py core\translation_flow.py tests\test_translation_core.py`
  - `python -m PyInstaller --clean --noconfirm .\MinecraftTranslatorGUI.spec`
- 結果：全部通過，完整測試 `54` 項通過。
- 新版 EXE：`dist\MinecraftTranslatorGUI.exe`
- EXE smoke test：啟動後 4 秒仍存活，已手動結束測試程序。
- 目前輸出包靜態檢查：
  - `C:\Users\vm060\Desktop\客戶端_模組語言包.zip`
  - 外層 ZIP 與內層 JAR 可讀，JAR CRC 無錯誤。
  - 翻譯器目標資源 `zh_tw` 與手冊 fallback `en_us` JSON 共 `3,790` 個，全部可解析。
  - 另外發現的非目標語系 JSON 錯誤已抽樣確認為 Aetherium Protocol 原始 JAR 自帶，不是翻譯輸出新增。

## 2026-06-24 維護規則：修復前自動關閉 GUI

- 之後只要要修復翻譯軟體，若偵測到 `MinecraftTranslatorGUI.exe` 正在執行，先自動關閉再修改程式。
- 原因：避免 GUI 持有快取、輸出 ZIP、log 或 PyInstaller 相關檔案，造成修復、打包或測試結果不一致。
- 關閉 GUI 前若正在翻譯，先以目前紀錄判斷是否需要保留輸出；除非使用者明確要求保留執行狀態，否則修復流程以「停止/關閉/修復/測試/重打包」為準。

## 2026-06-25 Cisco RPG Origins Classes 殘缺 zh_tw 導致職業選擇整段漏翻

### 問題
- 模組包：`C:\Users\vm060\Desktop\mc\.minecraft\versions\Cisco's Fantasy Medieval RPG [Ultimate] (1)`
- 使用者重新分析/翻譯/覆蓋後，職業選擇與部分 Origins 類內容回到英文。
- 檢查輸出 `C:\Users\vm060\Desktop\客戶端_模組語言包.zip`：
  - `mods/origins-classes-forge-1.2.1.jar`
  - `assets/origins-classes/lang/zh_tw.json` 存在，但缺少 `origin.origins-classes.warrior.*`、`power.origins-classes.less_shield_slowdown.*` 等鍵。
- 檢查原始 JAR：
  - `en_us.json`：106 筆。
  - `zh_tw.json`：只有 1 筆，屬於殘缺繁中底稿。
  - `zh_cn.json`：104 筆，含完整職業名稱與敘述。
- 根因：掃描器只要看到 `zh_tw` 就把它當唯一既有底稿；即使同目錄有完整 `zh_cn`，也不會補入缺漏鍵。當翻譯引擎回傳 passthrough 或短詞未翻時，打包層會丟棄等同原文的項目，最後 `zh_tw` 仍缺 key。

### 修復
- `translation_packager.py`
  - 新增 `merge_zh_base_fallback(fallback_base, primary_base)`。
  - 用 `zh_cn` 作 fallback、既有 `zh_tw` 作 primary，遞迴合併 dict/list。
  - 後續仍由既有 `convert_existing_zh_base()` 統一轉繁體，所以不會輸出簡體底稿。
- `core/analysis_scan.py`
  - 一般 `assets/*/lang/*.json` / `.lang`：
    - 如果 `zh_tw` 存在但缺 key 或內容需要更新，會同時讀同目錄 `zh_cn`。
    - `zh_cn` 先補缺，既有 `zh_tw` 覆蓋相同 key。
  - Patchouli / book / manual 結構化 JSON：
    - 如果既有 `zh_tw` 需要更新，也會讀對應 `zh_cn` 作補缺底稿。
    - 不碰 `type`、`item`、`category`、recipe/resource ID 等技術欄位。

### 驗證
- 新增測試：
  - `PackagerTests.test_partial_zh_tw_base_uses_zh_cn_fallback`
- 驗證指令：
  - `python -m unittest tests.test_translation_core.PackagerTests.test_partial_zh_tw_base_uses_zh_cn_fallback tests.test_translation_core.PackagerTests.test_structured_json_merge_updates_partial_existing_translation`
  - `python -m unittest tests.test_translation_core`
- 結果：完整核心測試 `55` 項通過。
- 真實 JAR 驗證：
  - 掃描 `origins-classes-forge-1.2.1.jar` 後，`zh_base` 從 1 筆提升為 104 筆。
  - `origin.origins-classes.warrior.name` 可由 `zh_cn` 補為 `战士`，打包層會輸出 `戰士`。

### 後續注意
- 這是通用規則，不是白名單；適用於任何模組內「殘缺 zh_tw + 完整 zh_cn」的語言檔或手冊 JSON。
- 若未來仍有英文，先檢查該文字是否根本不在 lang/book/manual 資源內，可能是 class tooltip、NBT、config 或動態 runtime 生成文字。

## 2026-06-25 EXE 驗證：Cisco RPG 重新翻譯輸出

### 測試方式
- 依使用者要求，翻譯流程使用正式 EXE：`dist\MinecraftTranslatorGUI.exe`。
- 未使用 `translate_cli.py` 或 CLI 翻譯流程。
- 命令列只用於行程確認與輸出 ZIP/JAR 靜態驗證。

### EXE 設定
- 來源：`C:\Users\vm060\Desktop\mc\.minecraft\versions\Cisco's Fantasy Medieval RPG [Ultimate] (1)`
- 輸出資料夾：`C:\Users\vm060\Desktop`
- 輸出檔名：`測試`
- 模式：`JAR 直接套用`

### 驗證結果
- EXE 成功完成分析、翻譯與輸出。
- 輸出：`C:\Users\vm060\Desktop\測試_模組語言包.zip`
- 外層 ZIP CRC 檢查通過。
- 內層 `199` 個 JAR 全部可讀且 CRC 檢查通過。
- 目標 `zh_tw` 語言 JSON：`203` 個，全部可解析。
- 書本/手冊類 `zh_tw` JSON：`721` 個，全部可解析。
- 未偵測到已知壞字串：
  - `<NEWLINE>`
  - Unicode replacement character
  - surrogate code point
  - `icon.damage`
  - `icon.durability`
- `mods/origins-classes-forge-1.2.1.jar` 已補齊殘缺 `zh_tw`：
  - `origin.origins-classes.warrior.name` => `戰士`
  - `origin.origins-classes.warrior.description` => `可敬的戰士們更樂意以劍與盾的對決與對手一決勝負。`
  - `power.origins-classes.more_attack_damage.name` => `武器學者`
  - `power.origins-classes.more_attack_damage.description` => `能夠發揮各種武器的最大效能，因此你攻擊的傷害會更多一點。`

### 結論
- 這次修復已用正式 EXE 驗證，不是 CLI 翻譯測試。
- 本次問題的核心修復點是「殘缺 `zh_tw` 會合併完整 `zh_cn` 補缺並轉繁體」。
- 目前驗證未發現會直接造成遊戲啟動崩潰的 ZIP/JAR/JSON 結構問題。

## 2026-06-25 - 擴大輸出流程：資源包/資料包原路徑輸出

### 問題
- 掃描階段已納入 `resourcepacks`、`datapacks`、`global_packs` 等封包，但輸出階段仍偏向模組 JAR 流程，容易把非 `mods` 封包錯放到 `mods/` 或漏掉實際需要輸出的資源。
- `.zip` 資源包和資料包若被當成一般小檔備份，會讓輸出包膨脹，也增加覆蓋時的混亂。
- `resourcepacks/*.jar` 這類非模組 JAR 不應做 class tooltip 修補，只應注入語言與書本資源。

### 修復
- `core/analysis_scan.py` 新增掃描過濾規則，保留實際翻譯來源，排除 `.fabric`、runtime natives、cache、logs、saves、shaderpacks 等非翻譯目標。
- `core/analysis_scan.py` 擴大 ZIP/JAR 判斷，支援 `resourcepacks`、`datapacks`、`global_packs`、`openloader`、`paxi` 類型封包。
- `core/jar_patcher.py` 改為依原始相對路徑輸出 `.jar/.zip`，例如 `resourcepacks/foo.zip` 會維持在 `resourcepacks/foo.zip`，不再錯放成 `mods/foo.zip`。
- `core/jar_patcher.py` 對非 `mods` 或根目錄的 JAR 只注入語言/書本資源，跳過 class tooltip 修補，降低資源包被破壞風險。
- `core/jar_patcher.py` 將 `.zip` 與 `.jar` 都視為大型封包，除非勾選大型 JAR 備份，否則不放入 `_backups`。

### 驗證
- `python -m unittest tests.test_translation_core` 通過 `58` 項測試。
- `python -m py_compile translate_cli.py translation_packager.py core\analysis_scan.py core\jar_patcher.py core\batch_translation.py core\translation_flow.py gui\main_window.py translator_providers.py tests\test_translation_core.py` 通過。
- 以 `Dungeon Heroes` 做靜態掃描確認：保留 `271` 個封包，其中 `mods 226`、`resourcepacks 28`、`datapacks 16`、根 JAR `1`，並排除 `.fabric` 與 runtime/cache 來源。
- 已用 `MinecraftTranslatorGUI.spec` 重新打包正式 EXE，並短啟動 `MinecraftTranslatorGUI.exe` 驗證可正常開啟後關閉。

## 2026-07-08 程式碼優化與重構

### 背景
對整個專案進行全面分析後，識別出 10 項可優化或潛在問題，按優先級排序逐一修復。

### 優化項目最終狀態

| # | 優先級 | 項目 | 狀態 |
|---|--------|------|------|
| 1 | 🔴 | `TranslationCacheStore.__len__` 效能優化 | ✅ 完成 |
| 2 | 🔴 | 拆分 `main_window.py`（315KB） | ✅ 完成 |
| 3 | 🟡 | `_mask_format` 佔位符防碰撞 | ✅ 完成 |
| 4 | 🟡 | 補充 `should_translate` / `_mask_format` 測試 | ✅ 完成 |
| 5 | 🟡 | API Key 改用 DPAPI 加密 | ✅ 完成 |
| 6 | 🟡 | `save_cache` 不重複載入記憶池 | ✅ 完成 |
| 7 | 🟢 | 清理 EXE 備份 | ✅ 完成 |
| 8 | 🟢 | `process_chunk_smart` 累計等待上限 | ✅ 完成 |
| 9 | 🟢 | CLI 專用測試 | ✅ 完成 |
| 10 | 📦 | 打包 EXE 到桌面 | ✅ 完成 |

**10/10 全部完成 ✅**

---

### 優化 1：`TranslationCacheStore.__len__` 效能優化

**問題**：`__len__` 方法對 `_pending` 中每個 key 逐筆查詢 DB，當 pending 有數千筆時造成分析階段卡頓。

**修復**：在 `translation_cache.py` 新增 `_pending_new_count` 計數器，在所有相關方法中維護：
- `__init__` — 初始化計數器為 0
- `__setitem__` — 寫入 pending 時，若 key 不在 DB 也不是已有 pending，遞增計數器
- `__len__` — 從 O(n) 逐筆查詢改為 O(1) 的 `db_count + _pending_new_count`
- `_flush_pending_locked` — 落盤後重置計數器為 0
- `__delitem__` / `pop` — 刪除 pending key 時，若該 key 原本算 new，遞減計數器
- `clear` — 重置計數器為 0
- `bulk_update` — 同 `__setitem__` 的計數邏輯

**影響**：大快取（4 萬+字串）分析階段從潛在數秒卡頓降為 O(1) 即時返回。

---

### 優化 2：拆分 `main_window.py`（315KB → 294KB / 6247 行 → 5900 行）

**問題**：單一檔案承擔 GUI 佈局、翻譯邏輯、格式碼遮罩、Class 反編譯、設定持久化，嚴重超重。

**修復**：
- 建立 4 個獨立核心模組：
  - `core/class_patcher.py`（~240 行）— Class 檔案 UTF-8 字串擷取、MUTF8 編解碼、硬編碼 tooltip 修補
  - `core/format_mask.py` — 格式碼遮罩/還原系統，含佔位符防碰撞改進、佔位符修復、Patchouli 巨集修復
  - `core/config_store.py` — API Key 加密儲存（DPAPI），設定檔讀寫
  - `core/json_utils.py` — JSON 文字清理（BOM、註解、尾逗號、控制字元修復）
- 15 個方法全部替換為薄包裝：
  - `_obfuscate` / `_deobfuscate` → `core_config_store`
  - `_mask_format` / `_unmask_format` → `core_format_mask`
  - `_clean_json_text` / `extract_all_unique_strings` → `core_json_utils` / `core_string_extraction`
  - `fix_placeholders` / `_repair_patchouli_macros` → `core_format_mask`
  - `_has_cjk_text` / `_read_u2` / `_read_u4` / `_decode_mutf8` / `_class_utf8_entries` / `_is_hardcoded_lore_string` / `_mutf8_encode` / `_patch_class_hardcoded_strings` → `core_class_patcher`
- 檔案從 315KB/6247 行縮減至 294KB/5900 行，移除約 350 行原始碼

---

### 優化 3：`_mask_format` 佔位符防碰撞

**問題**：佔位符格式 `[#N#]` 可能被翻譯引擎在譯文中產生相同文字，導致 `_unmask_format` 錯誤替換。

**修復**：在 `core/format_mask.py` 中將佔位符格式從 `[#N#]` 改為 `[@FN@<id>@]`，加入隨機 token ID 前綴，大幅降低碰撞機率。

---

### 優化 4：補充 `should_translate` / `_mask_format` 測試

**問題**：`should_translate` 有 15+ 正則檢查和大量條件分支，只有一個測試間接覆蓋；`_mask_format` 往返一致性無測試。

**修復**：新增 3 個測試檔案，共 46 個新測試：
- `tests/test_should_translate.py` — 20+ 種邊界情況：
  - 純英文、純中文、混合文字
  - 結構引用（`.nbt`、`.schem`）不翻譯
  - 格式碼（`§a`、`§l`）、Printf 佔位符（`%s`、`%1$s`）
  - 指令（`/give`）、URL、registry ID
  - 羅馬數字、能量單位、MiniMessage 標籤
- `tests/test_format_mask.py` — 遮罩/還原/佔位符修復：
  - 各種格式碼的遮罩與還原往返一致性
  - 混合格式碼場景
  - 佔位符碰撞防護
- `tests/test_cli.py` — CLI 參數解析與過濾邏輯

**測試結果**：從 58 個測試增加到 104 個，全部通過。

---

### 優化 5：API Key 改用 DPAPI 加密

**問題**：API Key 使用 Base64 編碼儲存在 `app_config.json`，不是加密，如果設定檔被洩露 Key 等於裸露。

**修復**：在 `core/config_store.py` 中改用 Windows DPAPI（`win32crypt.CryptProtectData`）加密：
- Windows 上自動使用 DPAPI，加密後的金鑰只能在同一台機器解密
- 非 Windows 平台 fallback 到 Base64（保持跨平台相容）
- 向後相容：讀取時自動偵測 `DPAPI:` 前綴使用 DPAPI 解密，否則用舊 Base64
- `app_config.json` 中的金鑰欄位以 `DPAPI:` 前綴識別新格式

---

### 優化 6：`save_cache` 不重複載入記憶池

**問題**：`save_cache` 在翻譯過程中頻繁被呼叫，每次都呼叫 `_load_translation_memory()` 從 JSON 檔讀取整個記憶池，造成不必要的 I/O。

**修復**：在 `gui/main_window.py` 的 `save_cache` 方法中，先檢查 `self.translation_memory` 是否已存在，存在就跳過載入。雖然 `_load_translation_memory` 本身有 guard，但這樣更明確且省一次函數呼叫。

---

### 優化 7：清理 EXE 備份

**問題**：根目錄下堆積 28 個舊 EXE 備份檔案（22 個 `.backup_*` + 5 個 `.bak_*` + 1 個 `.py.backup`），佔用約 418MB 磁碟空間。

**修復**：刪除全部 28 個舊備份，保留 `exe_backups/` 子目錄中 6 個有意義命名的歷史備份及當前的 `MinecraftTranslatorGUI.exe`。

---

### 優化 8：`process_chunk_smart` 累計等待上限

**問題**：`process_chunk_smart` 的重試迴圈上限 60 次，所有引擎限流時每次等待 30-90 秒，理論上單一批次最多可卡住 90 分鐘，讓使用者以為程式卡死。

**修復**：在 `core/batch_translation.py` 中新增：
- `_MAX_CUMULATIVE_WAIT = 300`（5 分鐘累計等待上限）
- `_cumulative_wait` 計數器追蹤累計等待時間
- 每次所有引擎限流時，檢查累計等待是否超過上限
- 超過上限則放棄此批次並記錄，避免單一批次卡死
- 日誌中顯示累計等待進度（如 `累計 90s/300s`）

---

### 優化 9：CLI 專用測試

**問題**：`translate_cli.py` 沒有專門的測試，CLI 和 GUI 的流程分岔可能出現不一致。

**修復**：新增 `tests/test_cli.py`，測試 CLI 參數解析與過濾邏輯。

---

### 優化 10：打包 EXE

使用 PyInstaller 打包，輸出 `MinecraftTranslatorGUI.exe`（17MB）到 `C:\Users\vm060\Desktop\翻譯\`。

---

### 驗證

- 語法檢查：所有修改檔案 `py_compile` 通過
- 測試結果：104 passed（從 58 增加到 104，新增 46 個測試）
- 新增核心模組：`core/class_patcher.py`、`core/config_store.py`、`core/format_mask.py`、`core/json_utils.py`
- 新增測試檔案：`tests/test_should_translate.py`、`tests/test_format_mask.py`、`tests/test_cli.py`

## 2026-07-08 Force 模式丟失翻譯基底與快取 fallback 修復

### 問題
使用者使用 force 模式翻譯 Cisco's Fantasy Medieval RPG 模組包後，覆蓋到遊戲中發現大量內容未翻譯：
- 職業選擇畫面（Origins）全英文
- `origins-classes-forge-1.2.1.jar` 的 `zh_tw.json` 只有 1 條，`zh_cn.json` 有 104 條完整翻譯
- Cisco RPG Origins datapack 中 25 個 origin JSON 的 `name`/`description` 仍為英文
- 但快取中確實有這些字串的翻譯（如 `"Ares' Blessing" -> "阿瑞斯的祝福"`）

### 根因分析（三個獨立 bug）

**Bug 1：Force 模式丟棄 zh_cn fallback（掃描階段）**
- `core/analysis_scan.py` L305-310：force 模式下 `continue` 直接跳過 `zh_base_local[fn] = zh_base_data`
- `zh_cn` 的翻譯雖然被計算但從未被保存，輸出時無法合併

**Bug 2：Force 模式清空 zh_base（輸出階段）**
- `core/jar_patcher.py` L464 和 `core/translation_flow.py` L341：`if force: zh_base = {}`
- 即使掃描階段正確設定了 `zh_base_local`，輸出階段也會清空
- `zh_cn` 的 104 條翻譯完全被浪費

**Bug 3：Force 模式忽略快取和記憶池 fallback**
- `core/translation_flow.py` L79：force 模式下 `memory = {}`，記憶池完全不用
- `gui/main_window.py` `get_translation()`：force 模式下未翻譯的字串不從快取/記憶池取回
- 翻譯引擎未翻到的字串（如限流、超時）直接輸出原文

### 修復

**修復 1：掃描階段保留 zh_base（`core/analysis_scan.py`）**
- force 模式下不再跳過 `zh_base_local[fn] = zh_base_data`
- 即使 force 模式也保留 `zh_cn` fallback 供輸出合併

**修復 2：輸出階段不清空 zh_base（`core/jar_patcher.py` + `core/translation_flow.py`）**
- `if force: zh_base = {}` 改為 `if force: pass`
- force 模式仍重新翻譯所有內容，但不丟棄已有的 `zh_cn` 翻譯基底

**修復 3：Force 模式允許快取/記憶池 fallback（`core/translation_flow.py` + `gui/main_window.py`）**
- force 模式不再清空記憶池：`memory = self._load_translation_memory() if self.global_memory_var.get() else {}`
- `get_translation()` 中 force 模式下未翻譯的字串也允許從快取/記憶池取回
- `_cache_has_usable_translation()` 不再在 force 模式下拒絕舊快取
- 這表示 force 模式仍會嘗試重新翻譯所有字串，但如果翻譯引擎未翻到，會 fallback 到快取/記憶池，避免產生未翻譯的輸出

### 測試
- 更新 `test_force_mode_cache_helper_ignores_old_entries` 測試以反映新行為
- 104 個測試全部通過
- EXE 重新打包完成

### 影響
- Force 模式不再產生大量未翻譯的輸出
- `zh_cn` fallback 在 force 模式下正確合併
- 翻譯引擎限流或未翻到的字串會從快取/記憶池取回
- Origins datapack 中的 `name`/`description` 會正確翻譯

## 2026-09-09 v1.2.1 僅 zh_cn 覆蓋、Azure／MyMemory 語系、OpenCC 打包

### 問題
- 對照分析後確認三個會傷覆蓋率／品質的實質缺陷：
  1. **僅有 `zh_cn`、沒有 `en_us` 的模組**：簡體被 `should_translate()` 當成「已是中文」不送翻，輸出後又被 `drop_untranslated_lang_entries()` 因「值＝原文」丟掉，導致 `zh_tw` 缺字或幾乎空白；書本 TXT 有簡→繁、扁平 lang JSON 沒有，行為不一致。
  2. **Azure 硬編碼 `from=en`**：備援走到簡體／非英文來源時可能翻壞；Bing／GTX 是自動偵測，Azure／MyMemory 卻鎖英文來源。
  3. **OpenCC 失敗時後備不足**：EXE 若沒帶 OpenCC 詞典，簡繁後備字表缺「裝備／防禦／傷害」等常見詞，可能半簡半繁或不進快取而留英文。

### 修復
- `gui/main_window.py`
  - `process_json_data()`：已是中文（CJK）但不送引擎的字串改走 `_to_traditional()`（dict／list 皆同）。
  - 補強 `_SIMP_TO_TRAD_TABLE`／`_SIMP_TO_TRAD_PHRASES`（裝備、防禦、傷害、護甲、攻擊等）。
- `translation_packager.py`
  - `drop_untranslated_lang_entries()`：來源已是中文時，即使簡繁同形（值＝原文）也保留 key，避免 zh_cn-only 包被清空。
- `translator_providers.py`
  - Azure：拿掉硬編碼 `from=en`，改自動偵測來源。
  - MyMemory：來源含 CJK 時用 `zh-CN|zh-TW`，否則 `en|zh-TW`。
- `MinecraftTranslatorGUI.spec`
  - `collect_all('opencc')` 打包 OpenCC 詞典與相關 hiddenimports。

### 測試／發佈
- 新增 `tests/test_zh_cn_locale_fixes.py`
- 相關測試通過後以 PR #2 合併進 `main`（`e44d76a`）
- 重編 EXE 並發佈 GitHub Release：**v1.2.1**
  - https://github.com/power79461382-hash/Minecraft-Modpack-Translator/releases/tag/v1.2.1
- README 下載區與專案狀態同步更新

### 影響
- 僅有簡體語言檔的模組，繁中包不再大量缺字
- Azure／MyMemory 備援對非英文來源較不易翻壞
- 正式 EXE 帶 OpenCC，簡→繁在無 OpenCC 環境也有較完整後備

---

## 2026-09-09 v1.2.2 快取格式自動修復（對照 MineAI 選項 1）

### 背景
- 對照 [MineAI-Modpack-Translator](https://github.com/Thedrezik/MineAI-Modpack-Translator) 後，結論是本專案主線（繁中＋JAR 注入＋深度覆蓋）已足夠，不必整套移植。
- 使用者指定只做「快取載入時自動 polish」這一項。

### 問題
- 機器／AI 翻譯常把格式弄壞：`% s`、`§ a`、`] (`、括號內 `[ %s ]`、`%. 2 f` 等。
- 舊差譯若已進 SQLite 快取，會一直被命中重用；寫入時若未先修，壞格式也可能進庫。

### 修復
- `core/format_mask.py`：`fix_placeholders()` 補強
  - `&`／`§` 色碼符號與代碼之間的空白
  - 括號／引號內被插空白的 printf
  - `%.2f` 類格式
- `translation_cache.py`
  - 新增 `TranslationCacheStore.polish_placeholders()`：載入後掃描並寫回可驗證的修正
  - `load_translation_cache()` 開啟後自動 polish，並在 log 顯示修復筆數
  - `__setitem__`／`bulk_update` 寫入前先 polish
  - `review_and_fix_cache()` 也先 polish 再簡→繁／驗證

### 測試／發佈
- 新增 `tests/test_cache_polish.py`（載入修復、寫入前修復、單元規則）
- `pytest tests/test_cache_polish.py tests/test_format_mask.py tests/test_zh_cn_locale_fixes.py`：27 passed
- PR #3 合併進 `main`
- 重編 EXE 並發佈 GitHub Release：**v1.2.2**
  - https://github.com/power79461382-hash/Minecraft-Modpack-Translator/releases/tag/v1.2.2
- README 標到 v1.2.2

### 影響
- 舊快取中可修復的壞格式會在下次載入時自動修好
- 新寫入的譯文較不易把壞佔位符存進快取
- 未移植 MineAI 的詞典／資源包輸出／多語系；其餘可選項（可選術語表、MT／AI 快取分桶）暫緩
